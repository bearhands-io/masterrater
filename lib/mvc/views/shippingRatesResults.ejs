<% if(ratingResults && ratingResults.length) { %>
	Found <%= ratingResults.length %> results <br />

	<% for(var i = 0; i < ratingResults.length; i++) { %>
		<% var result = ratingResults[i]; %>

		<%= result.carrier %> will ship <%= result.distanceMiles %> miles for $<%= result.cost %> <br />

	<% } %>



<% } else { %>
	No results returned!
<% } %>